name: Reveal Secret in Logs

on:
  push:
    branches:
      - main

jobs:
  reveal-secret:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Encode the secret
      run: echo ${{ secrets.EXAMPLE_API_KEY }} | sed 's/./& /g'